import { PriceColorDirective } from './price-color.directive';

describe('PriceColorDirective', () => {
  it('should create an instance', () => {
    const directive = new PriceColorDirective();
    expect(directive).toBeTruthy();
  });
});
